import { Button } from "@/components/ui/button";

const Page = () => {
  return (
    <div>
      <Button className="text-sm md:text-base">Hello world</Button>
    </div>
  );
};

export default Page;
